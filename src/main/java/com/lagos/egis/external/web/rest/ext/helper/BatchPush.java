package com.lagos.egis.external.web.rest.ext.helper;

import com.lagos.egis.external.domain.Batch;


public class BatchPush {

    String pushBatch(Batch batch){

        // com.landsystems.lrs.model.Batch b = new  com.landsystems.lrs.model.Batch();
        // RegistryOffice ro = new RegistryOffice();
        // ro.setId(b.getId());
        // b.setOffice(ro);
        // Transaction tra = new Transaction();
        // Dictionary dicTransType = new Dictionary();
        // tra.setTransactionType();
        // b.getTransactions().add(tra);

        // //b.getOffice().setId(batch.getOfficeId());

        return null;

    }

}
